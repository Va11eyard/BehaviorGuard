# Notebooks

Placeholder for a future demo notebook.

Do not add notebooks with fabricated results. Any notebook added here should
load committed artifacts from `results/primary/` or re-run
`scripts/sequential_ato_study.py` against cached scores.
